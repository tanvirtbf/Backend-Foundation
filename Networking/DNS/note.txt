DNS (Domain Name System) কীভাবে কাজ করে — একদম শুরু থেকে শেষ পর্যন্ত

আমরা প্রতিদিনই ব্রাউজারে গিয়ে কোনো না কোনো ওয়েবসাইটে প্রবেশ করি — যেমন, **www.example.com**।
 কিন্তু আপনি কি জানেন, এই ডোমেইন নামটি টাইপ করার পর আসলে “পর্দার আড়ালে” কতগুলো ধাপ ঘটে?
চলুন দেখি, কিভাবে একটি সাধারণ domain name থেকে আমাদের কম্পিউটার সার্ভারের সাথে connect হয় এবং ওয়েবসাইটের ডেটা নিয়ে আসে।👇

1️⃣ Domain Name থেকে IP Address খোঁজার মূল ধারণা

ইন্টারনেটের প্রতিটি ওয়েব সার্ভারের একটি IP Address থাকে (যেমন: 192.168.10.1 বা 142.250.183.110)।
কিন্তু মানুষ সংখ্যা মনে রাখতে পারে না, তাই আমরা ব্যবহার করি domain name — যেমন www.google.com বা www.example.com।
এই নামকে IP তে রূপান্তর করার কাজটাই করে DNS System (Domain Name System)।

অর্থাৎ DNS হলো ইন্টারনেটের “Phonebook” — যেখানে নামের পাশে সংশ্লিষ্ট IP Address লেখা থাকে।

2️⃣ প্রথম ধাপ: Browser Cache চেক করে

যখন আপনি www.example.com লিখে Enter চাপেন —
আপনার browser (যেমন Chrome, Firefox) প্রথমে নিজস্ব cache এ খোঁজে:

“এই ডোমেইনের জন্য কোনো IP Address আগেই মনে রাখা আছে কি?”

যদি browser cache এ থাকে, তাহলে সরাসরি সেই IP তে request পাঠানো হয় — ফলে ওয়েবসাইট খুব দ্রুত লোড হয়।
না থাকলে, পরের ধাপে যায়।

3️⃣ দ্বিতীয় ধাপ: Operating System (OS) Cache এ খোঁজে

Browser এ না পেলে, এখন Operating System (Windows, macOS, Linux ইত্যাদি)-এর DNS Cache চেক করা হয়।
OS cache এ আগের কোনো সময় resolve করা IP address থাকলে সেটিই ব্যবহার করা হয়।
তবুও না পেলে, এবার যায় পরের ধাপে — DNS Resolver এর কাছে।

4️⃣ তৃতীয় ধাপ: DNS Resolver (ISP / Router)

এখন request চলে যায় আমাদের ISP (Internet Service Provider) বা Router এর DNS Resolver এর কাছে।
এটিও একটি বিশেষ সফটওয়্যার সার্ভিস, যার কাজ হলো অন্য সার্ভারগুলোর সাথে যোগাযোগ করে আসল IP Address বের করা।
প্রথমে এটি নিজস্ব cache চেক করে দেখে — পূর্বে কেউ এই domain resolve করেছিল কিনা।
যদি cache এ না থাকে, তখন এটি যায় Root DNS Server এর কাছে।

5️⃣ চতুর্থ ধাপ: Root Server — ইন্টারনেটের শীর্ষ পর্যায়

Root Server হচ্ছে DNS System-এর সবচেয়ে উপরের স্তর।
সারা বিশ্বে মোট ১৩টি Root Server Cluster আছে (A থেকে M পর্যন্ত নামকরণ করা)।
এরা জানে না example.com এর IP Address কী, কিন্তু জানে “.com” নামের TLD Server (Top Level Domain Server) কোথায় আছে।
তাই Root Server বলে দেয় —

“তুমি .com এর TLD Server এর কাছে যাও, ওরা জানে example.com কোথায় আছে।”

6️⃣ পঞ্চম ধাপ: TLD Server (.com, .org, .net ইত্যাদি)

Root Server নির্দেশ দেওয়ার পর DNS Resolver এখন .com এর TLD (Top-Level Domain) Server এর কাছে যায়।
এই সার্ভারে .com ending থাকা সমস্ত domain এর রেজিস্ট্রেশন তথ্য সংরক্ষিত থাকে।
যেমন: example.com, google.com, facebook.com ইত্যাদি।

যখন আমরা GoDaddy, Namecheap, Cloudflare বা অন্য কোনো domain registrar থেকে domain কিনি, তখন তারা এই TLD Server এ আমাদের domain সম্পর্কিত nameserver information রেজিস্টার করে দেয়।
তাই .com এর TLD Server জানে example.com কোন Nameserver এ হোস্ট করা আছে।

7️⃣ ষষ্ঠ ধাপ: Authoritative Name Server

TLD Server তখন বলে দেয়:

“example.com এর nameserver হলো ns1.examplehosting.com”

এখন DNS Resolver সেই Authoritative Name Server এ query পাঠায়।
এই Nameserver এ domain এর আসল রেকর্ড থাকে — যেমন A record, MX record, CNAME record ইত্যাদি।
Authoritative Server তখন example.com এর আসল IP Address (যেমন 142.250.183.110) দিয়ে দেয়।

8️⃣ সপ্তম ধাপ: Cache এবং Response ফেরত আসে

DNS Resolver এখন প্রাপ্ত IP Address টি cache করে রাখে, যেন পরের বার আবার খুঁজতে না হয়।
তারপর সেই IP Browser এবং OS এর কাছে পাঠানো হয়।
Browser সেই IP Address এ গিয়ে সার্ভারের সাথে সংযোগ স্থাপন করে এবং ওয়েবসাইটের HTML, CSS, JS ফাইল ইত্যাদি লোড করে ব্যবহারকারীর সামনে দেখায়।

🧭 পুরো প্রক্রিয়া এক নজরে (Flow)
Browser Cache 
  ↓
OS Cache 
  ↓
DNS Resolver (ISP / Router)
  ↓
Root Server
  ↓
TLD Server (.com / .org / .ai)
  ↓
Authoritative Name Server
  ↓
Actual Web Server (Website Data)

🧠 সারসংক্ষেপে:

DNS System হলো ইন্টারনেটের অদৃশ্য নেভিগেশন ম্যাপ,
যা আমাদের টাইপ করা “নাম” কে রূপান্তর করে “সংখ্যায়িত ঠিকানা” তে (IP Address),
এবং সেই পথে নিয়ে যায় ওয়েবসাইটের আসল সার্ভার পর্যন্ত।

DNS না থাকলে, আমাদের প্রতিটি ওয়েবসাইটে যেতে হতো এরকম IP দিয়ে —
142.250.183.110 — যা নিশ্চয়ই কারো মনে রাখা সম্ভব নয়! 😅